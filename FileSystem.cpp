//
// Created by mbrozek on 27.12.20.
//

#include "FileSystem.h"



bool FileSystem::formatSystem(const std::string &systemName, const std::string &systemSize) {
    FILE* file;
    int system_size;

    system_size = parseSize(systemSize);

    return false;
}

bool FileSystem::loadSystem(const std::string &systemName) {
    return false;
}

int FileSystem::parseSize(const std::string &systemSize) {

    return 0;
}
